import React from "react";
import ReactDOM from "react-dom/client";
import PublicRuntime from "./PublicRuntime.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PublicRuntime />
  </React.StrictMode>
);